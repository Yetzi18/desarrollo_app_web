console.log("Conexión Establecida correctamente con js!");
// Crea una función que multiplique los elementos ubicados en posiciones pares (0, 2, 4) de un arreglo. Muestra el resultado con un alert.

function multiplicaPares(){
    let lis = [1, 2, 3, 4, 5];
    let multi = (lis[0] * lis[2] * lis[4]);
    alert("El resultado es: " + multi);
}